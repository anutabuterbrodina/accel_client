export interface IRequisites {
    legalName: string | null,
    address: string | null,
    INN: string | null,
    OGRN: string | null,
    KPP: string | null,
    OKPO: string | null,
    BIK: string | null,
}