export class AbstractFiltersFormatter {
    protected static readonly a

    protected static format(sorting: ISorting): ISorting {

        return
    }
}

interface ISorting {
    sortOption: string,
    sortOrder: string,
}